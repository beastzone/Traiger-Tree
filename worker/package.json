{
  "name": "traiger-tree-api",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc",
    "db:local": "wrangler d1 execute traiger-tree --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  },
  "private": true
}
